[project]
name = "tinywmcraft-task"
version = "0.1.0"
description = "Interactive world-model research task for Craftax-Classic"
requires-python = ">=3.11"
dependencies = [
  "numpy==2.4.6",
  "jax==0.10.1",
  "flax==0.12.7",
  "imageio==2.37.3",
]

[project.optional-dependencies]
cuda = ["jax[cuda12]==0.10.1"]

[tool.uv]
package = false
